# 实验日志

## RFC 模块残差分支消融实验

本实验参考 RFC-yolov8 论文中的 RFC 模块设计思路，在 RGB-IR 双模态 YOLOv8n 的 `ResCGAFF+P2` 模型上，研究
P3 融合模块残差分支的特征预处理方式对检测性能的影响。

### 实验目的

ResCGAFF 的 P3 融合由原始信息残差支路和 Concat-Gated AFF 注意力支路组成。保持注意力支路、P2 分支、检测头和
训练配置不变，仅改变残差支路中 RGB 与红外特征在拼接前的处理方式，验证局部特征重标定或上下文建模是否有效。

### 对照版本

| 版本 | 残差分支 | 实验意图 | 配置文件 |
| --- | --- | --- | --- |
| C（Baseline） | `Conv1x1(Concat(Frgb, Fir))` | 保留原始双模态细节并统一混合压缩 | `custom_models/yolov8n-dual-rescgaff-p2-residual-c.yaml` |
| A | `Conv1x1(Concat(Conv1x1(Frgb), Conv1x1(Fir)))` | 对两路特征分别进行通道对齐和轻量重标定 | `custom_models/yolov8n-dual-rescgaff-p2-residual-a.yaml` |
| B | `Conv1x1(Concat(Conv3x3(Frgb), Conv3x3(Fir)))` | 在融合前增强每个模态的局部上下文建模 | `custom_models/yolov8n-dual-rescgaff-p2-residual-b.yaml` |

三个版本均在拼接后保留原有的 `Conv1x1` 投影，以保证残差支路输出通道数与注意力支路一致，能够执行残差相加。

### 控制变量

- Backbone：`ResCGAFF+P2` 双分支 Backbone。
- 检测尺度：P2、P3、P4、P5 四尺度检测头。
- 注意力支路：Concat-Gated AFF 保持不变。
- 残差系数：`alpha=0.1`。
- 数据集划分、输入尺寸、训练轮数、优化器、学习率、数据增强和随机种子保持一致。

### 评价指标

记录总体 Precision、Recall、mAP50、mAP50-95，以及 People、Car、Bus、Motorcycle、Lamp、Truck 各类别 AP。
同时记录参数量、GFLOPs、推理速度和训练/验证损失曲线。建议每个版本使用至少 3 个随机种子训练，比较均值和标准差。

### 结果记录

| 版本 | 种子 | Precision | Recall | mAP50 | mAP50-95 | 参数量 | 推理速度 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C |  |  |  |  |  |  |  |  |
| A |  |  |  |  |  |  |  |  |
| B |  |  |  |  |  |  |  |  |

### 结论

待实验完成后，根据多随机种子结果判断：若 A 或 B 相比 C 有稳定提升，说明对应的逐模态预处理有效；若没有稳定提升，
则当前性能瓶颈不在残差支路的预处理方式，应转而分析 Neck 特征融合或 Backbone 特征提取。
